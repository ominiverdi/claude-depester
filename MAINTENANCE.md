# Maintenance Status

claude-depester is retired.

The maintainer no longer uses Claude Code, and this project relies on patching third-party binaries/extensions that change without notice. Continuing responsibly would require active dogfooding, testing against upstream releases, and security review that the maintainer cannot provide.

No new features, fixes, or Claude Code compatibility updates are planned.
